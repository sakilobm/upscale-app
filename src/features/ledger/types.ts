export type { LedgerDirection, LedgerStatus, PartialReturn, LedgerEntry } from '@store/ledgerStore';
export type { LoanType, Loan } from '@store/loansStore';

export type LedgerTab = 'owed_to_me' | 'i_owe' | 'loans';
